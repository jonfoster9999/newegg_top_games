require "spec_helper"

describe NeweggTopGames do
  it "has a version number" do
    expect(NeweggTopGames::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
